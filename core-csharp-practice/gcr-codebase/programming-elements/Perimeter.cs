using System;

class Perimeter
{
    static void Main(string[] args)
    {
        double length = 10;    // fixed length
        double breadth = 5;    // fixed breadth

        double perimeter = 2 * (length + breadth);

        Console.WriteLine("Perimeter of Rectangle = " + perimeter);
    }
}
